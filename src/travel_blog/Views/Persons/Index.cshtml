@model IEnumerable<travel_blog.Models.Person>

@{
    ViewData["Title"] = "Persons Page";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
    <h4>@Html.DisplayNameFor(model =>model.name)</h4>
    <ul>
        @foreach(var person in Model)
        {
            <li>@Html.DisplayFor(modelItem => person.name) | @Html.ActionLink("Details", "Details", new { id = person.id }) | @Html.ActionLink("Delete", "Delete", new { id = person.id }) | @Html.ActionLink("Edit", "Edit", new { id = person.id })</li>

        }
    </ul>

<p>@Html.ActionLink("Add new location", "Create") </p>
